<!DOCTYPE html>
<html>
<head>
  <% include ../partials %>
</head>

<body>

<% include ../partials %>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.id %> - <%= r.name %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
